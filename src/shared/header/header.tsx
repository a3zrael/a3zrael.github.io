import React from 'react';
import { Logo } from '../logo/logo';

import './header.css';

export const Header = () => {
  return (
    <div className="header-container">
      <Logo />
    </div>
  );
};
